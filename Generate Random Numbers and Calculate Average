let n = Number(prompt("How many random numbers?"));
let sum = 0;
let result = "";

for (let i = 0; i < n; i++) {
    let num = Math.random();
    sum += num;
    result += num + "<br>";
}

document.write("Random Numbers:<br>" + result);
document.write("Average = " + (sum / n));
